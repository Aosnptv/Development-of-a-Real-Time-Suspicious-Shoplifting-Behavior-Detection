# 1. ติดตั้งระบบเฟรมเวิร์กหน้าเว็บแดชบอร์ดหลัก
pip install streamlit

# 2. ติดตั้งระบบจัดการภาพและประมวลผลวิดีโอกล้องวงจรปิด
pip install opencv-python

# 3. ติดตั้งสมองกล AI โมเดลระดับโลก (YOLOv11 ตรวจสอบวัตถุ ท่าทาง และโครงกระดูก)
pip install ultralytics

# 4. ติดตั้งโมดูลสำหรับการรับ-ส่งข้อมูลโครงข่าย (ใช้ส่งภาพและแจ้งเตือนเข้า Telegram Bot)
pip install requests

# 5. ติดตั้งระบบจัดการตารางและข้อมูลประวัติเหตุการณ์ย้อนหลัง
pip install pandas

streamlit run app.py